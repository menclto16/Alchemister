<Page x:Class="Alchemister.AdventurePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Alchemister"
      mc:Ignorable="d" 
      d:DesignHeight="700" d:DesignWidth="1000"
      Title="AdventurePage"
      Background="#31004a">
    <Page.Resources>
        <Style TargetType="{x:Type Button}">
            <Setter Property="Background" Value="#373737" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="FontSize" Value="15" />
            <Setter Property="Width" Value="300" />
            <Setter Property="Height" Value="50" />
            <Setter Property="Margin" Value="5" />
        </Style>
    </Page.Resources>
    <StackPanel VerticalAlignment="Center">
        <Button Click="Button_Click1">Level 1</Button>
        <Button Click="Button_Click2">Level 2</Button>
        <Button Click="Button_Click3">Level 3</Button>
        <Button Click="Button_Click4">Level 4</Button>
        <Label x:Name="Message" HorizontalAlignment="Center" Height="100" FontSize="40"></Label>
        <WrapPanel x:Name="RewardsPanel" Height="80" HorizontalAlignment="Center">
        </WrapPanel>
    </StackPanel>
</Page>
